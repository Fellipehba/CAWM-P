# --- núcleo do Preparador (necessário) ---
streamlit>=1.30
numpy>=1.26
pandas>=2.0
geopandas>=1.0
shapely>=2.0
folium>=0.15
streamlit-folium>=0.18
openpyxl>=3.1
requests>=2.31
tqdm>=4.65

# --- aquisição ANA sem token (validar localmente; ver nota no README) ---
# hydrobr>=0.1.1

# --- ETP automática via ERA5-Land/CDS (rodar LOCAL; pesado p/ Cloud) ---
# cdsapi>=0.7
# xarray>=2024.0
# netcdf4>=1.6
